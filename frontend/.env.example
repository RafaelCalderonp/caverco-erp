# URL completa del backend en producción (incluye /api/v1).
# En desarrollo local se puede omitir: se usa el proxy de Vite hacia http://localhost:8000.
# En Cloudflare Pages, configurar como variable de entorno del proyecto.
VITE_API_URL=https://caverco-erp-backend.onrender.com/api/v1
